export const requiredBeats = ['inbox-accounted', 'judge-comparison']

export async function workflow({ page, web, mark, pause, filmed, motion }) {
  throw new Error('Define a product workflow before recording.')
}
